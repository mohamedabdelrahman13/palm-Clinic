import { DisableCopiesDirective } from './disable-copies.directive';

describe('DisableCopiesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableCopiesDirective();
    expect(directive).toBeTruthy();
  });
});
